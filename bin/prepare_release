#!/usr/bin/env ruby
# frozen_string_literal: true

require "digest"
require "fileutils"
require "rbconfig"
require "rubygems/package"

class ReleasePreparer
  DEFAULT_RUNNER = ->(command, root) { system(*command, chdir: root) }
  DEFAULT_BUILDER = lambda do |root|
    Dir.chdir(root) do
      specification = Gem::Specification.load("jekyll-slides.gemspec")
      raise Gem::InvalidSpecificationException, "cannot load jekyll-slides.gemspec" unless specification

      FileUtils.mkdir_p("pkg")
      path = File.join("pkg", specification.file_name)
      Gem::Package.build(specification, false, true, path)
      checksum = Digest::SHA256.file(path).hexdigest
      File.write("#{path}.sha256", "#{checksum}  #{File.basename(path)}\n")
      true
    end
  end

  def initialize(root: File.expand_path("..", __dir__), ruby: RbConfig.ruby, runner: DEFAULT_RUNNER, builder: DEFAULT_BUILDER, stderr: $stderr)
    @root = root
    @ruby = ruby
    @runner = runner
    @builder = builder
    @stderr = stderr
  end

  def call
    commands.each do |command|
      next if @runner.call(command, @root)

      @stderr.puts "Release preparation failed: #{command.join(' ')}"
      return false
    end

    return true if @builder.call(@root)

    @stderr.puts "Release preparation failed: build jekyll-slides.gemspec"
    false
  rescue StandardError => e
    @stderr.puts "Release preparation failed: #{e.message}"
    false
  end

  private

  def commands
    [
      [@ruby, "-S", "bundle", "exec", "rake"],
      [@ruby, "-S", "bundle", "exec", "rake", "yard"],
      [@ruby, File.join(@root, "bin", "generate_llm.rb")]
    ]
  end
end

exit(ReleasePreparer.new.call ? 0 : 1) if $PROGRAM_NAME == __FILE__
